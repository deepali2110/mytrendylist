<%= form_with(model: product) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :p_name %>
    <%= form.text_field :p_name %>
  </div>

  <div class="field">
    <%= form.label :p_price %>
    <%= form.text_field :p_price %>
  </div>

  <div class="field">
    <%= form.label :p_dprice %>
    <%= form.text_field :p_dprice %>
  </div>

  <div class="field">
    <%= form.label :p_details %>
    <%= form.text_field :p_details %>
  </div>

  <div class="field">
    <%= form.label :p_image %>
    <%= form.text_field :p_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
